DROP SCHEMA IF EXISTS EJERCICIO_12;
CREATE SCHEMA EJERCICIO_12;
USE EJERCICIO_12;

# DEPARTAMENTO (CODIGO, NRP)
CREATE TABLE DEPARTAMENTO(
	CODIGO INT PRIMARY KEY AUTO_INCREMENT,
    NRP INT 
);


# PROFESOR (NRP, NOMBRE, AREA_CONOCIMIENTO, CATEGORIA, CODIGO_DEPARTAMENTO)
CREATE TABLE PROFESOR (
	NRP INT PRIMARY KEY AUTO_INCREMENT,
    NOMBRE VARCHAR (50),
    AREA_CONOCIMIENTO VARCHAR (50),
    CATEGORIA VARCHAR (50),
    CODIGO_DEPARTAMENTO INT,
    
    FOREIGN KEY (CODIGO_DEPARTAMENTO) REFERENCES DEPARTAMENTO (CODIGO)
    
);

ALTER TABLE DEPARTAMENTO 
ADD CONSTRAINT FOREIGN KEY (NRP) REFERENCES PROFESOR (NRP);


# GRUPO (CODIGO, MAX, TIPO)
CREATE TABLE GRUPO (
	CODIGO INT PRIMARY KEY AUTO_INCREMENT,
    MAX_ALUMNOS SMALLINT,
    TIPO VARCHAR (50)
);

CREATE TABLE G
# GRUPO_TEORIA (CODIGO_GRUPO, HORAS_CLASE)
# GRUPO_PRACTICA (CODIGO_GRUPO, TALLER)
# ALUMNO (DNI, FECHA_NACIMIENTO, NOMBRE, BECA)
# ASIGNATURA (CÓDIGO, NOMBRE, CREDITO, CARACTER, CURSO)
CREATE TA
# PROFESOR_IMPARTE_GRUPO (NRP, CODIGO_GRUPO)
# GRUPO_CONTIENE_ASIGNATURA (CODIGO_GRUPO, CÓDIGO_ASIGNATURA)
# MATRICULA (DNI, CÓDIGO_ASIGNATURA, CODIGO_GRUPO)





